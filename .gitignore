/target/
.idea/
.vscode/
.settings
.project
.classpath

*.iml
.DS_Store

# The following files are generated/updated by vaadin-maven-plugin
node_modules/
src/main/frontend/generated/
vite.generated.ts

# Claude Code configuration is committed with this branch.
# A global ~/.gitignore_global rule excludes `.claude`, so re-include it here --
# the directory must be un-ignored before anything inside it can be.
!.claude/
!.claude/**
# ...except personal overrides.
.claude/settings.local.json
