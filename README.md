# My Application

A Spring Boot + Vaadin project. Build your UI in pure Java — no HTML, no JavaScript.

> **New to Vaadin?** The 5-minute [Quickstart](https://vaadin.com/quickstart) walks you from here to your first running app, a live code change, and an AI-assisted edit with Copilot.

---

## Fastest start — no plugin needed

From the project folder:

```bash
./mvnw spring-boot:run        # Windows: mvnw.cmd spring-boot:run
```

Then open **http://localhost:8080**.

The first start takes ~30 seconds while Maven downloads dependencies.

> **Port 8080 already in use?** Stop the other process, or set `server.port=8081` in `src/main/resources/application.properties` and open that port instead.
>
> **To stop the app:** press `Ctrl+C` in the terminal (or the red Stop button if you launched from your IDE).

## Optional upgrade — instant hotswap

Running with `spring-boot:run` works, but Java code changes need a server restart. For **live reload** — edit Java, see it in the browser without restarting — install the **Vaadin plugin** and start the app through it:

- **IntelliJ IDEA:** install *Vaadin* from the JetBrains Marketplace → **Debug using Hotswap Agent** (dropdown next to Run). *Just installed it? Let IntelliJ finish indexing, or restart it, if the menu item isn't there yet.*
- **VS Code:** install the *Vaadin* extension → **Vaadin: Debug using Hotswap Agent** from the command palette.
- **Eclipse:** install the *Vaadin* plugin → right-click the project → **Run As → Vaadin Application**.

This is what makes the edit-and-see-it loop feel instant — and it's required for the AI edits in [Vaadin Copilot](https://vaadin.com/docs/latest/tools/copilot).

---

## Ask your AI assistant about Vaadin (optional)

If you use Claude Code, Cursor, or another AI coding assistant, connect it to the **Vaadin MCP server** so it answers against real Vaadin docs and the exact API of your installed version — instead of guessing from outdated training data.

```bash
# One-time setup — see https://vaadin.com/docs/latest/building-apps/mcp
```

A `.mcp.json` is included (commented out by default). Uncomment it, or run the setup command above, to activate.

---

## Build for production

```bash
./mvnw package
java -jar target/*.jar
```

## Learn more

- [Vaadin Quickstart](https://vaadin.com/quickstart) — the 5-minute getting-started path
- [Components](https://vaadin.com/docs/latest/components) — 50+ UI components, all callable from Java
- [Vaadin Copilot](https://vaadin.com/docs/latest/tools/copilot) — visual + AI editing in the browser
- [Full documentation](https://vaadin.com/docs)

---

## Demo: this feature was generated by Claude Code

The `MessageView` in this project was not written by hand. It was produced in a
single Claude Code session from the prompt below — including the layout, the
navigation and the tests.

This branch differs from `02-vanilla` in one way: Claude had the **Vaadin MCP
server** connected, so it looked up the Vaadin 25.2 API from the official docs
and component API instead of relying on training data.

### The prompt

> Create a new view with a text field and a button. On click, a notification
> with the content of the field should appear. The button should be primary,
> and the notification should be a success message in the bottom right. Empty
> input should be handled. Add the view to the navigation and write a test for it.

### What Claude produced

| File | Purpose |
| --- | --- |
| `src/main/java/dev/vaadin/agentdemo/views/MessageView.java` | The view: `TextField` + primary `Button`, shows the field content as a success notification in the bottom right corner. |
| `src/main/java/dev/vaadin/agentdemo/views/MainLayout.java` | `@Layout` `AppLayout` with a drawer; the `SideNav` is built from `MenuConfiguration.getMenuEntries()`, so every `@Menu` view appears automatically. |
| `src/test/java/dev/vaadin/agentdemo/views/MessageViewTest.java` | Three browserless tests covering the behaviour below. |

Behaviour:

- Clicking the button opens a `Notification` with the field's content,
  `Position.BOTTOM_END` and `NotificationVariant.SUCCESS`.
- The button uses `ButtonVariant.PRIMARY`.
- Empty or whitespace-only input does **not** show a success notification.
  Instead the field is marked invalid with an error message, gets focus back,
  and an error-variant notification appears in the same corner. Typing again
  clears the error state.
- The view is annotated with `@Menu`, so it shows up in the side navigation
  without touching the layout.

Two decisions Claude made and reported:

- The project had **no views at all** — not even a layout. "Add the view to the
  navigation" therefore meant creating the navigation shell (`MainLayout`) as
  well, not just registering a menu entry.
- The view is mapped to `@Route("")`. It is the only view in the app, so a
  dedicated path would have left `/` as a 404 on startup.

### What the MCP server changed

The Vaadin MCP server answered against Vaadin 25.2 specifically, which matters
here because Vaadin 25 renamed the theme variant constants:

- `ButtonVariant.PRIMARY` and `NotificationVariant.SUCCESS` — **not** the
  `LUMO_`-prefixed names that older training data would suggest.
- `com.vaadin.browserless.SpringBrowserlessTest` as the browserless test base
  class, together with its `navigate()` / `test()` / `find()` API.
- The `@Layout` + `MenuConfiguration.getMenuEntries()` pattern for automatic
  layouts, straight from the current "Add a Router Layout" guide.

Claude still cross-checked a few details against the sources jars in `~/.m2`
before writing the code.

### Verification

Everything was verified before the work was reported as done:

```bash
./mvnw test   # 3 tests, 0 failures, 0 errors
```

Claude additionally wrote a throwaway test to confirm that the view really
renders inside `MainLayout` and that the side navigation lists the `Message`
entry, then deleted it again.

The tests use `browserless-test-spring`, which is already a dependency of this
project — no browser and no running server required.

### Cost of the generation

| Metric | Value |
| --- | --- |
| Model | Claude Opus 5 (1M context) |
| Wall-clock time | 4m 43s |
| API time | 3m 2s |
| Input tokens | 1.5k |
| Output tokens | 12.7k |
| Cache read / write tokens | 1.9m / 60.2k |
| Cost | 1.89 $ |
| Tool calls | ~30 (Vaadin MCP docs and component API, plus a few lookups in the sources jars in `~/.m2`) |

> Numbers taken from `/cost` in the Claude Code session.
