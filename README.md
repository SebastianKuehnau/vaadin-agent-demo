# My Application

A Spring Boot + Vaadin project. Build your UI in pure Java — no HTML, no JavaScript.

> **New to Vaadin?** The 5-minute [Quickstart](https://vaadin.com/quickstart) walks you from here to your first running app, a live code change, and an AI-assisted edit with Copilot.

---

## Fastest start — no plugin needed

From the project folder:

```bash
./mvnw spring-boot:run        # Windows: mvnw.cmd spring-boot:run
```

Then open **http://localhost:8080**.

The first start takes ~30 seconds while Maven downloads dependencies.

> **Port 8080 already in use?** Stop the other process, or set `server.port=8081` in `src/main/resources/application.properties` and open that port instead.
>
> **To stop the app:** press `Ctrl+C` in the terminal (or the red Stop button if you launched from your IDE).

## Optional upgrade — instant hotswap

Running with `spring-boot:run` works, but Java code changes need a server restart. For **live reload** — edit Java, see it in the browser without restarting — install the **Vaadin plugin** and start the app through it:

- **IntelliJ IDEA:** install *Vaadin* from the JetBrains Marketplace → **Debug using Hotswap Agent** (dropdown next to Run). *Just installed it? Let IntelliJ finish indexing, or restart it, if the menu item isn't there yet.*
- **VS Code:** install the *Vaadin* extension → **Vaadin: Debug using Hotswap Agent** from the command palette.
- **Eclipse:** install the *Vaadin* plugin → right-click the project → **Run As → Vaadin Application**.

This is what makes the edit-and-see-it loop feel instant — and it's required for the AI edits in [Vaadin Copilot](https://vaadin.com/docs/latest/tools/copilot).

---

## Ask your AI assistant about Vaadin (optional)

If you use Claude Code, Cursor, or another AI coding assistant, connect it to the **Vaadin MCP server** so it answers against real Vaadin docs and the exact API of your installed version — instead of guessing from outdated training data.

```bash
# One-time setup — see https://vaadin.com/docs/latest/building-apps/mcp
```

`.mcp.json` here is deliberately empty: the Vaadin docs tools, the curated skills and the
enforcing hooks are all wired up in `.claude/settings.json` instead — see
[`.claude/README.md`](.claude/README.md) for what each branch configures.

---

## Run the agent in a Docker sandbox

`.sbx/kit/spec.yaml` is a [Docker Sandboxes](https://docs.docker.com/ai/sandboxes/) kit
(`schemaVersion: "2"`, `kind: mixin`). It runs Claude Code in a container behind a network
allowlist instead of on the host:

```bash
sbx run claude --kit ./.sbx/kit .
```

The agent configuration is *not* in the kit. `.claude/` is committed and the workspace is
mounted read-write, so the hooks, the permissions allowlist and the
`vaadin-view-conventions` skill are already inside the sandbox — and the base `claude` kit
accepts the trust dialog for the workspace, so they take effect without a prompt. The kit
only adds what that configuration depends on:

| What it adds | Why |
| --- | --- |
| `claude plugin install vaadin-skills` and `vaadin-agent-tools` | `.claude/settings.json` only *enables* them; a fresh container still has to fetch them. |
| `./mvnw -B -ntp test` at create time | The `Stop` hook runs `./mvnw -o test` — **offline**. A fresh sandbox has an empty `~/.m2`, so without pre-warming that hook would block every turn. |
| `permissions.network.allow` | Maven Central plus `maven.vaadin.com` (the pom's `vaadin-directory` repository), GitHub for the plugin installs, and `vaadin.com` / `mcp.vaadin.com` / `www.javadocs.dev` for the plugins' MCP servers. |
| `JAVA_HOME` | The base image ships a JDK but leaves it unset. |

No `apt-get` step is needed: the image already ships `jq` (which every hook builds its
JSON reply with) and `unzip`, and `/usr/lib/jvm/default-java` is JDK 25.

> Tests only — no browser (the tests are browserless) and no npm (`build-frontend` binds
> to `prepare-package`). To reach the app from the host, add a top-level
> `ports: [{container: 8080, name: app}]` plus `registry.npmjs.org`, `nodejs.org` and
> `tools.vaadin.com`.
>
> **The Figma workflow below does not run in here yet** — it needs an interactive
> Figma login. Run it on the host.

---

## Build for production

```bash
./mvnw package
java -jar target/*.jar
```

## Learn more

- [Vaadin Quickstart](https://vaadin.com/quickstart) — the 5-minute getting-started path
- [Components](https://vaadin.com/docs/latest/components) — 50+ UI components, all callable from Java
- [Vaadin Copilot](https://vaadin.com/docs/latest/tools/copilot) — visual + AI editing in the browser
- [Full documentation](https://vaadin.com/docs)

---

## Demo: these features were generated by Claude Code

Nothing under `dev/vaadin/agentdemo/` was written by hand. `MainLayout` and
`MessageView` came out of earlier Claude Code sessions; `CustomerManagementView`
was generated on this branch straight from a Figma design, against the
conventions those earlier sessions left behind.

### What `07-figma` added

The earlier branches added *knowledge* (docs MCP, Vaadin skills), *enforcement*
(hooks) and *taste* (a project-local conventions skill). This branch adds a
**design source**: the agent reads the Figma file and builds the view from it.

**The design** — the selected Figma frame, node `1:825`:

![Figma frame "Grid view": the heading Customer Management with a description and
two actions, a scope tab bar with All, Recent and Favourite, a search field next to
Status and Advanced buttons, and a four-column customer table inside a bordered
card headed "Customers (42)"](docs/images/figma.png)

**The result** — `/customers` in the running app:

![The same screen rendered by Vaadin: the view name in the navbar and in the
drawer next to Message, Aura-themed tabs, a search field with Status and Advanced,
and a Grid inside an outlined card headed "Customers (42)" listing twelve
customers](docs/images/vaadin-view.png)

The heading appears exactly once, in the navbar — that is the design decision
described below. The two visible differences are the theme, not the
implementation: Aura draws the selected tab as a filled pill instead of Figma's
underline, and its search field is outlined instead of filled grey. The mock is
Lumo-based; this app is Aura.

Two pieces have to be set up by hand — neither comes with the checkout:

| Step | What |
| --- | --- |
| Install the Figma plugin | `claude plugin install figma@claude-plugins-official` — deliberately *not* in `.claude/settings.json`, because it needs an interactive Figma login |
| Create the skill | `.claude/skills/figma-to-vaadin/SKILL.md` — write it yourself: read the design context first, resolve the component annotations, review the component docs, implement last |

Then, per view:

1. Select the view or component in your Figma design.
2. Run `/figma-to-vaadin implement the selected view on the figma design to a new CustomerManagementView`
3. Paste the design link when the agent asks for it.

> **Three limits worth knowing up front**
>
> - **No Docker sandbox yet** — the Figma login is interactive, so run this on the host.
> - **A Figma login is required.** Claude Code guides you through it in the terminal.
> - **The design link must be pasted by hand.** The agent cannot read your current
>   Figma selection; it needs a node-specific URL (`…?node-id=1-825`).

The interesting part is where the skill and the project disagreed. Three of its
rules had to be overridden:

| The skill prescribes | What this project actually needs |
| --- | --- |
| `LumoUtility` classes and `--lumo-*` | This app is Aura. `LumoUtility` names would render with **no** backing CSS — broken layout, not merely the wrong theme. Replaced by `--vaadin-*` properties in a view stylesheet. |
| Icon-only button = `theme="tertiary icon"` | `ButtonVariant.ICON` does not exist, only `LUMO_ICON`. `TERTIARY` alone is right; `Button.setIcon()` adds `theme=icon` by itself. |
| Icons `lumo:play` / `lumo:plus` / `lumo:search` | Vaadin 25 no longer bundles Lumo icons without `@Theme`. Same glyphs from `VaadinIcon`. |

Also dropped: the design's own "Customer Management" heading, because `MainLayout`
already renders the view name in the navbar from `@Menu`.

Still active from the branches below: both Vaadin plugins, the permissions
allowlist, the `vaadin-view-conventions` skill and the three hooks
(`SessionStart` project facts, `PostToolUse` Aura/Lumo check, `Stop` test gate) —
see [`.claude/README.md`](.claude/README.md) for the full branch matrix.

### Current state

Paths relative to `src/{main,test}/java/dev/vaadin/agentdemo/`:

| File | Purpose |
| --- | --- |
| `base/ui/MainLayout.java` | `@Layout` `AppLayout` whose `SideNav` is built from `MenuConfiguration.getMenuEntries()`, so every `@Menu` view appears automatically. |
| `message/ui/view/MessageView.java` | `TextField` + primary `Button`; shows the field content as a success notification in the bottom right. |
| `customer/domain/Customer.java` | Record — name, email, last purchase, organization: the design's four columns. |
| `customer/service/CustomerService.java` | Mock `@Service`: the design's twelve sample rows, plus the total of 42 that the card header reports. |
| `customer/ui/view/CustomerManagementView.java` | Scope `Tabs`, a search `TextField` with two filter buttons, and a `Grid` inside an outlined `Card` whose header carries the row count and an overflow button. Presentational only — no listeners. |
| `customer/ui/view/customer-management-view.css`¹ | Page inset, section rhythm, tab underline, card padding reset — all through `--vaadin-*`. |
| `base/ui/MainLayoutTest.java` | Parameterized over both routed views, located by the route derived from `MenuEntry`. |
| `message/ui/view/MessageViewTest.java` | Five browserless tests. |
| `customer/ui/view/CustomerManagementViewTest.java` | Eleven browserless tests: menu registration, column count, row data, the spelled-out date, the card's count, tab labels and selection, action labels, primary/tertiary variants, accessible names. |

¹ Under `src/main/resources/META-INF/resources/`, `@import`ed from `styles.css`.

`./mvnw test` — 20 tests green. `HelloAgentWorldView` and its test were removed on
this branch, so `MainLayoutTest` was repointed at the new view.

---

## Cost across the branches

The same feature request was run once per branch, against the same project,
with an increasing amount of Vaadin-specific context. Each branch's README has
the details; this is the comparison.

| Branch | Setup | Wall-clock | Cost | Tests |
| --- | --- | --- | --- | --- |
| `01-chat-client` | claude.ai chat UI, code pasted by hand | — | not measured | 3 |
| `02-vanilla` | Claude Code, no Vaadin context | 15m 56s | 3.60 $ | 6 |
| `03-MCP` | + Vaadin docs MCP server | 4m 43s | 1.89 $ | 3 |
| `04-skills` | + Vaadin Skills plugin | 14m 2s | 1.90 $ | 5 |
| `05-tools-hooks` | + agent tools, hooks, permissions | 10m 36s | 6.01 $ | 5 |
| `06-custom-skills` | + project-local conventions skill | 5m 34s | 4.27 $ | 16* |

\* Two views are under test on this branch, not one: the existing `MessageView`
and the new `HelloAgentWorldView`, plus the parameterized layout test covering both.

What the numbers show:

- **The chat UI has no measurable token cost** — it runs under the subscription,
  and `/cost` never sees it. It also produced the least: no layout, so the
  `@Menu` entry it wrote has nothing to render it, and `/` stays a 404.
- **The MCP server pays for itself.** Without it the agent burns tokens
  rediscovering the Vaadin 25 API from the jars — nearly twice the cost of
  `03-MCP` for a result that still lands in a flat `views/` package.
- **Skills cost about the same as MCP alone** and add what the API docs do not
  carry: feature-based packaging and theme awareness.
- **`05-tools-hooks` is the expensive one, and the hooks are not the reason.**
  The context hook and the permissions allowlist are cheap — they remove
  searching and approval round-trips. The cost sits in what the harness made
  affordable: verifying every API signature against the jars, then starting the
  app and checking the result in a real browser. Roughly 3× the price of
  `04-skills` for the same five tests — and the only run in the table where
  anyone actually saw the feature work.

Test counts are not a quality ranking on their own: `02-vanilla` wrote the most
tests and still needed the most tokens to get there.

> Numbers taken from `/cost` in each Claude Code session. Wall-clock includes
> thinking, tool calls and the agent waiting on Maven.

---

## What a Figma run costs

`07-figma` is deliberately **not** a row in the table above, and its numbers are
**not meant to be compared** with it. It ran a different task (a customer
management screen from a Figma file, not the greeting view), from a different
input, with three views under test instead of two. It is here for one reason
only: to show what a Figma-driven session actually costs.

| | |
| --- | --- |
| Cost | **8.69 $** |
| Duration | 11m 51s API, 17m 1s wall-clock |
| Model | `claude-opus-5` — 55.0k output, 1.6k input, 9.7m cache read, 248.2k cache write |
| Prompt cache | 48 requests, 97 % of input tokens served from cache, no misses |
| Code produced | ~430 lines — view 141, test 151, service 66, stylesheet 54, record 14 |
| Tests | 20 green |

Two things drive that number: the Figma frame arrives as one large payload, and the
skill demands the *complete* documentation for every component before any code is
written — Button, Tabs, Text Field, Card and Grid, plus the javadoc for four enum
types. Hence the 9.7m cached input tokens. What it bought is in the table above:
all three Lumo-vs-Aura conflicts surfaced before the first line was written.

> **The `/cost` line "25 lines added, 14 lines removed" is wrong** — it understates
> the output by more than tenfold. In auto mode the agent writes files through the
> shell (`cat > file <<EOF`), which `/cost` does not register as a code change; the
> same blind spot the `Bash` hook matcher exists for. The ~430 lines above are
> counted from the working tree instead.
